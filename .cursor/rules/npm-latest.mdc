---
alwaysApply: true
---

# npm Package Installation

When installing npm packages, always install the latest version by appending `@latest` to the package name.

For example: `yarn add some-package@latest`
