import { defineNitroConfig } from 'nitro/config'

export default defineNitroConfig({
  // Preset is configured in vite.config.ts
  // This file can be used for additional Nitro-specific configuration
})
